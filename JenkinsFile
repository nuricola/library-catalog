pipeline {
    agent any

    stages {
        stage('Build & Test') {
            steps {
                sh 'mvn -B test'
            }
        }
    }

    post {
        always {
            junit 'target/surefire-reports/*.xml'
        }
    }
}
